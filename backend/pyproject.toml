[project]
name = "embodied-operating-data-system"
version = "0.1.0"
description = "Offline data management service for embodied robot operation tasks."
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115,<1.0",
    "uvicorn[standard]>=0.30,<1.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0,<9.0",
    "httpx>=0.27,<1.0",
]
